import Head from "next/head";
import Layout from "../../components/layout";

import React from 'react'

export const settings = () => {
  return (
    <div>
        <Layout>
            <h2>Desde settings</h2>
        </Layout>
    </div>
  )
}

export default settings